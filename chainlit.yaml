chainlit: 2.4.1

# Interface settings
ui:
  name: "W0!z Chatbot"
  discription: "A Simple Chatbot Powered by Google's Gemini Model"

# Message settings
default_expand_messages: true

# Auth settings
auth:
  required: true
  providers: 
    - github

# OAuth Configuration
oauth_providers:
  github:
    client_id: ${OAUTH_GITHUB_CLIENT_ID}
    client_secret: ${OAUTH_GITHUB_CLIENT_SECRET} 